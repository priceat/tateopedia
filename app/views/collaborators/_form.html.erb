<%= form_for([@wiki, @collaborator]) do |f| %>
  <% if @collaborator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collaborator.errors.count, "error") %> prohibited this collaborator from being saved:</h2>

      <ul>
      <% @collaborator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select 'user_id', options_for_select(@users.collect{ |u| [u.email, u.id] }) %>
  </div>
</br>
  <div class="actions">
    <%= f.submit "Add Collaborator" %>
  </div>
<% end %>